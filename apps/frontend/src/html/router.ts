import { element } from "./element";
import htmlConfig from "./htmlConfig";
import createElement from "./createElement";

const html = {
  htmlConfig,
  element,
  createElement,
};

export default html;
